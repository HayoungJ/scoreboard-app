import { AppRegistry } from 'react-native';
import ScoreScreen from './app/screens/ScoreScreen';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => ScoreScreen);
